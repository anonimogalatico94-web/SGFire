name: SGFire Runner Test

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Runner test
        run: |
          echo "SGFire Actions runner OK"
          test -f project.godot
